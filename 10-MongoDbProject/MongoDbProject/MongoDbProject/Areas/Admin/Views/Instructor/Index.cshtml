@using MongoDbProject.Dtos.InstructorDtos
@model List<ResultInstructorDto>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/_AdminLayout.cshtml";
    int count = 1;
}

<div class="card-header">
    <h5>Instructors</h5>
    <div class="card-header-right">
        <div class="btn-group card-option">
            <button type="button" class="btn dropdown-toggle btn-icon" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <i class="feather icon-more-horizontal"></i>
            </button>
            <ul class="list-unstyled card-option dropdown-menu dropdown-menu-right">
                <li class="dropdown-item full-card"><a href="#!"><span><i class="feather icon-maximize"></i> maximize</span><span style="display:none"><i class="feather icon-minimize"></i> Restore</span></a></li>
                <li class="dropdown-item minimize-card"><a href="#!"><span><i class="feather icon-minus"></i> collapse</span><span style="display:none"><i class="feather icon-plus"></i> Expand</span></a></li>
            </ul>
        </div>
    </div>
</div>
<div class="card-body">

    <table class="table table-bordered">
        <tr>
            <th>#</th>
            <th>Profile Image</th>
            <th>Name Surname</th>
            <th>Title</th>
            <th>Delete</th>
            <th>Update</th>
        </tr>
        @foreach (var tutors in Model)
        {
            <tr>
                <td>@count</td>
                <td><img src="@tutors.ImageUrl" style="width:75px; height:75px" /></td>
                <td>@tutors.FullName</td>
                <td>@tutors.Title</td>
                <td><a asp-action="DeleteInstructor" asp-route-id="@tutors.InstructorId" asp-area="Admin" class="btn btn-danger">Delete</a></td>
                <td><a asp-action="UpdateInstructor" asp-route-id="@tutors.InstructorId" asp-area="Admin" class="btn btn-info">Update</a></td>
            </tr>
            count++;
        }
    </table>

    <a asp-action="CreateInstructor" asp-controller="Instructor" asp-area="Admin" class="btn btn-primary">Add New Instructor</a>
</div>